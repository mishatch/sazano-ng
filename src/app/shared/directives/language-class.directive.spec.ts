import { LanguageClassDirective } from './language-class.directive';

describe('LanguageClassDirective', () => {
  it('should create an instance', () => {
    const directive = new LanguageClassDirective();
    expect(directive).toBeTruthy();
  });
});
